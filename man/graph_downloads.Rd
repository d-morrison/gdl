% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_downloads.R
\name{graph_downloads}
\alias{graph_downloads}
\title{Plot package download counts over time by source}
\usage{
graph_downloads(package, ...)
}
\arguments{
\item{package}{Character; CRAN package name to graph.}

\item{...}{
  Arguments passed on to \code{\link{.get_download_data}}
  \describe{
    \item{\code{github_repo}}{Character string \code{"owner/name"} for a
GitHub repository to also include release-asset
downloads from, or \code{NULL} (default) for CRAN only.}
    \item{\code{new}}{Logical; include new (daily) downloads?
Defaults to \code{TRUE}.}
    \item{\code{cumulative}}{Logical; include cumulative downloads?
Defaults to \code{TRUE}.}
    \item{\code{start}}{Start date for the plot (a \link{Date} or string
coercible to one). Defaults to \code{NULL} (all available
data).}
    \item{\code{unit}}{Character string specifying the time unit to
aggregate by. One of \code{"day"}, \code{"week"}, \code{"month"},
\code{"quarter"}, or \code{"year"}. Defaults to \code{"month"}.}
    \item{\code{title}}{Character string for the plot title. Defaults
to a description including the package name and time
unit. Set to \code{NULL} to omit.}
  }}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object with faceted panels.
}
\description{
Fetches download data from CRAN (via \code{packageRank}) and
optionally GitHub Releases (via \code{gh}), then plots new and
cumulative downloads.
}
\details{
GitHub release downloads are cumulative counts per release
asset from the GitHub API. New GitHub downloads are derived
as the contribution of each release. CRAN downloads are
fetched via \code{\link[packageRank:cranDownloads]{packageRank::cranDownloads()}}.

Requires the \code{packageRank} package (and \code{gh} if
\code{github_repo} is set), listed under \code{Suggests}.
}
\examples{
\dontshow{if (interactive() || nzchar(Sys.getenv("GDL_DOCS_BUILD"))) withAutoprint(\{ # examplesIf}
graph_downloads("dplyr")
graph_downloads("dplyr", github_repo = "tidyverse/dplyr")
graph_downloads("dplyr", unit = "week", start = "2024-01-01")
\dontshow{\}) # examplesIf}
}
