% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_download_data.R
\name{.get_download_data}
\alias{.get_download_data}
\title{Fetch and prepare download data for plotting}
\usage{
.get_download_data(
  package,
  github_repo = NULL,
  new = TRUE,
  cumulative = TRUE,
  start = NULL,
  unit = c("month", "day", "week", "quarter", "year"),
  title,
  ...
)
}
\arguments{
\item{package}{Character; CRAN package name to graph.}

\item{github_repo}{Character string \code{"owner/name"} for a
GitHub repository to also include release-asset
downloads from, or \code{NULL} (default) for CRAN only.}

\item{new}{Logical; include new (daily) downloads?
Defaults to \code{TRUE}.}

\item{cumulative}{Logical; include cumulative downloads?
Defaults to \code{TRUE}.}

\item{start}{Start date for the plot (a \link{Date} or string
coercible to one). Defaults to \code{NULL} (all available
data).}

\item{unit}{Character string specifying the time unit to
aggregate by. One of \code{"day"}, \code{"week"}, \code{"month"},
\code{"quarter"}, or \code{"year"}. Defaults to \code{"month"}.}

\item{title}{Character string for the plot title. Defaults
to a description including the package name and time
unit. Set to \code{NULL} to omit.}

\item{...}{
  Arguments passed on to \code{\link[packageRank:cranDownloads]{packageRank::cranDownloads}}
  \describe{
    \item{\code{when}}{\code{last-day}, \code{last-week} or \code{last-month}.
If this is given, then \code{from} and \code{to} are ignored.}
    \item{\code{from}}{Start date as \code{yyyy-mm-dd}, \code{yyyy-mm} or \code{yyyy}.}
    \item{\code{to}}{End date as \code{yyyy-mm-dd}, \code{yyyy-mm} or \code{yyyy}.}
    \item{\code{check.package}}{Logical. Validate and "spell check" package.}
    \item{\code{dev.mode}}{Logical. Use validatePackage0() to scrape CRAN.}
    \item{\code{fix.cranlogs}}{Logical. Use RStudio logs to fix 8 dates with duplicated data in 'cranlogs' results.}
    \item{\code{pro.mode}}{Logical. Faster but fewer checks/features. Closer to cranlogs::cran_downloads() but with cranDownloads()'s plot method.}
  }}
}
\value{
A \code{download_data} tibble (subclass of
\code{tbl_df}) with columns \code{date}, \code{provider}, \code{metric},
and \code{downloads}, plus attributes \code{title},
\code{github}, and \code{multi_metric}.
}
\description{
Fetches CRAN (and optionally GitHub) download data,
then combines, filters, and pivots into long format.
}
\keyword{internal}
